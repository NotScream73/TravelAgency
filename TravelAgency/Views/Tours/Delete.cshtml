@model TravelAgency.ViewModels.Tours.TourDeleteViewModel

@{
    ViewData["Title"] = "Удаление тура";
}

<h1>Удаление тура</h1>
<hr />
<h3>Вы действительно хотите удалить тур?</h3>
<div>
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(_ => Model.Item.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(_ => Model.Item.Name)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="@Model.Item.Id" />
        <input type="submit" value="Удалить" class="btn btn-danger" /> |
        <a asp-action="Index">Назад</a>
    </form>
</div>
